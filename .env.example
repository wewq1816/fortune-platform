# 운세 플랫폼 환경 변수 템플릿
# 이 파일을 .env로 복사하고 실제 값을 입력하세요

# Claude API 설정
CLAUDE_API_KEY=your_claude_api_key_here
CLAUDE_MODEL=claude-3-haiku-20240307

# 비용 설정
CLAUDE_MONTHLY_BUDGET=50000
CLAUDE_COST_PER_1K_INPUT_TOKENS=0.00025
CLAUDE_COST_PER_1K_OUTPUT_TOKENS=0.00125

# 캐싱 설정
ENABLE_CACHE=true
CACHE_TTL_SECONDS=86400

# JWT Secret (랜덤하고 긴 문자열로 변경 필수!)
JWT_SECRET=your_very_long_and_random_secret_key_here_minimum_32_characters

# 마스터 코드 (무제한 사용 코드 - 프로덕션에서 변경 필수!)
MASTER_CODE=your_master_code_here

# MongoDB 설정
MONGO_URL=mongodb://localhost:27017
DB_NAME=fortune_platform

# CORS 허용 도메인 (쉼표로 구분)
ALLOWED_ORIGINS=http://localhost:3000

# Session Secret (랜덤하고 긴 문자열로 변경 필수!)
SESSION_SECRET=your_session_secret_key_here_minimum_32_characters

# 개발 환경
NODE_ENV=development
PORT=3000
